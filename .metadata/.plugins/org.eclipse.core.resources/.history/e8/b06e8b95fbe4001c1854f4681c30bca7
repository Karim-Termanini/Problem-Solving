import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
		Scanner in;
		in = new Scanner(System.in);
		char ch = in.next().charAt(0);
		int askchar = ch;
		int newask = askchar + 1;
		System.out.println(ch == 'z' ? 'a' : (char) newask);
	}

}