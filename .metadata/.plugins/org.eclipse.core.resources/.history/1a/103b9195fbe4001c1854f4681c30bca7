import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
		Scanner in;
		in = new Scanner(System.in);
		char ch = in.next().charAt(0);
		int askchar = ch;
		System.out.println(ch == 'z' ? 'a' : (char) (askchar + 1));
	}

}